import React from 'react';
import './floatingBot.css';

const FloatingBot = ({ onClick }) => {
  return (
    <div className="floating-bot" onClick={onClick} title="Ask AI">
      
    </div>
  );
};

export default FloatingBot;
